package com.ghozix.idlegenerators.config;

/** Estado por generador en la config (v1.3.0): DEFAULT delega en el toggle de su categoría. */
public enum GeneratorToggle {
    DEFAULT,
    ON,
    OFF
}
