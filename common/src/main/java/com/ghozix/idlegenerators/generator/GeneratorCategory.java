package com.ghozix.idlegenerators.generator;

/** Categoría de un generador para los toggles de config (v1.3.0). */
public enum GeneratorCategory {
    ORES,
    WOODS,
    STONES,
    COLORS,
    NATURE
}
